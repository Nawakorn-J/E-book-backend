const express = require("express");
const publishingHouseController = require("../controllers/publishingHouseController");
const router = express.Router();

// router.post("/", publishingHouseController.createPublishingHouse);

module.exports = router;
